#include <stdio.h>

int main()
{
    int a, b;
    printf("Enter first number: ");
    scanf("%d", &a);
    printf("Enter the value of second number: ");
    scanf("%d", &b);
    printf("The sum of these two numbers is %d", a + b);
    return 0;
}
// Create a program to add two numbers in c --> Try it yourself!