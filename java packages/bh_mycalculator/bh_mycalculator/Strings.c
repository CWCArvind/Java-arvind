#include<stdio.h>

int main(){
  /*  char Str[] = "ARVIND";
    char *ptr = Str;
    while(*ptr!='\0'){
        printf("%c",*ptr);
        ptr++;
    }
    */

    // Convinient way to print the string : 

 //   printf("\n%s\n",Str);
 char *ptr = "Let's code whole day and night";
 printf("%s",ptr); 

   

    return 0;
}